{
	"manifest_version": 2,
	"name": "CSS Selector Counter",
	"version": "1.0",
	"description": "Counts the total elements with a specific CSS selector on the current page.",
	"browser_action": {
		"default_icon": "icon.png",
		"default_popup": "popup.html"
	},
	"permissions": [
		"activeTab"
	]
}
